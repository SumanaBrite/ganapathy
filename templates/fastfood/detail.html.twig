{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}
    <h1>Liste des articlesFastFood</h1>
    <br></br>
<div class="jumbotron">
    <table class="table table table-bordered table-striped">
        <tbody>
        <tr>
                <th>Image</th>
                <img src="/images/uploads/{{ articleFastFood.path }}" class="h-100 w-100 align-center" alt="{{ articleFastFood.image }}">
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ articleFastFood.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ articleFastFood.description }}</td>
            </tr>          
            <tr>
                <th>Prix</th>
                <td>{{ articleFastFood.prixUnitaire }}</td>
            </tr>
        </tbody>
    </table>
    </div>

    <a href="{{ path('fastfood') }}" class="btn btn-secondary btn-lg">Retour à la liste</a>
    <br></br>
    
    <br></br>

   
{% endblock %}
