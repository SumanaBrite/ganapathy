<form method="post" action="{{ path('taux_tva_delete', {'id': taux_tva.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taux_tva.id) }}">
    <button class="btn">Delete</button>
</form>
