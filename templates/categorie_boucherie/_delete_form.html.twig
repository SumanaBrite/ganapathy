<form method="post" action="{{ path('categorie_boucherie_delete', {'id': categorie_boucherie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_boucherie.id) }}">
    <button class="btn">Delete</button>
</form>
