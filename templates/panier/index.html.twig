{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
<div class ="p-3">
<h1>Votre Panier</h1>
<div class= "heading-underline"></div>
</div>
{% if items | length > 0 %}
<table class="table">
<thead>
    <tr>
        <th>Produit</th>
        <th>Prix</th>
        <th>Quantité</th>
        <th>Total</th>
        <th>Supprimer</th>
    </tr>
</thead>
<tbody>

{% for item in items %}
    <tr>
        <td>{{item.product.nom}}</td>
        <td>{{item.product.prixUnitaire}}</td>
        <td>{{item.quantity}}</td>
        <td>{{item.product.prixUnitaire * item.quantity }}</td>
        <td>
            <a href="{{ path('panier_delete', {'id':item.product.id}) }}" class ="btn btn-danger btn-sm">
            <i class="fas fa-trash"></i>
            </a>

        </td>
    </tr>
{% endfor %}

</tbody>
<tfoot>
  <tr>
      <td colspan="3" class="text-right"> Total :</td>
      <td>{{ total }}</td>
  </tr>
  
</tfoot>
</table>
{% else %}
<p>Votre Panier est vide😢</p>
{% endif %}
{% endblock %}
