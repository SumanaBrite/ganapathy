<form method="post" action="{{ path('categorie_fast_food_delete', {'id': categorie_fast_food.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_fast_food.id) }}">
    <button class="btn">Delete</button>
</form>
