{% extends 'base.html.twig' %}

{% block title %}UniteFastFood{% endblock %}

{% block body %}
    <h1>UniteFastFood</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unite_fast_food.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ unite_fast_food.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unite_fast_food_index') }}">back to list</a>

    <a href="{{ path('unite_fast_food_edit', {'id': unite_fast_food.id}) }}">edit</a>

    {{ include('unite_fast_food/_delete_form.html.twig') }}
{% endblock %}
