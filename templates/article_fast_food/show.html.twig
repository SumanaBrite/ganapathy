{% extends 'base.html.twig' %}

{% block title %}ArticleFastFood{% endblock %}

{% block body %}
    <h1>ArticleFastFood</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_fast_food.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ article_fast_food.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article_fast_food.description }}</td>
            </tr>
            <tr>
                <th>PrixUnitaire</th>
                <td>{{ article_fast_food.prixUnitaire }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ article_fast_food.path }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ article_fast_food.image }}</td>
            </tr>
            <tr>
                <th>Promotion</th>
                <td>{{ article_fast_food.promotion ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>PrixPromotion</th>
                <td>{{ article_fast_food.prixPromotion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('article_fast_food_index') }}">back to list</a>

    <a href="{{ path('article_fast_food_edit', {'id': article_fast_food.id}) }}">edit</a>

    {{ include('article_fast_food/_delete_form.html.twig') }}
{% endblock %}
